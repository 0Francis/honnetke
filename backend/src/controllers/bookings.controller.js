// Sprint 4 — Student Features / Sprint 5 — Provider Features
// TODO: implement getBookings, createBooking, updateBookingStatus

const getBookings = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 4/5' });
};

const createBooking = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 4' });
};

const updateBookingStatus = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 5' });
};

module.exports = { getBookings, createBooking, updateBookingStatus };
