// Sprint 2 — Listings Core
// TODO: implement getListings, getListingById, createListing, updateListing,
//        deleteListing, deactivateListing, reactivateListing, uploadImages

const getListings = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 2' });
};

const getListingById = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 2' });
};

const createListing = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 2' });
};

const updateListing = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 2' });
};

const deleteListing = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 2' });
};

const deactivateListing = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 2' });
};

const reactivateListing = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 2' });
};

const uploadImages = async (req, res, next) => {
  res.status(501).json({ message: 'Not implemented — Sprint 2' });
};

module.exports = {
  getListings,
  getListingById,
  createListing,
  updateListing,
  deleteListing,
  deactivateListing,
  reactivateListing,
  uploadImages,
};
